[project]
name = "janatpmp"
version = "0.1.0"
description = "Janat Project Management Platform"
requires-python = ">=3.14"
dependencies = ["gradio[mcp]>=6.0.2", "pandas"]

[tool.setuptools]
packages = ["features", "features.inventory"]
